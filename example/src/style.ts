import { StyleSheet } from 'react-native';

export const lineChartStyles = StyleSheet.create({
  axisStyle: {
    color: 'white',
  },
  container: {
    flex: 1,
    marginTop: 30,
  },
  labelStyle: {
    color: 'white',
  },
});
