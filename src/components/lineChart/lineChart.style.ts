import { StyleSheet } from 'react-native';

export const lineChartStyle = () => {
  return {
    canvasStyles: StyleSheet.create({
      canvasContainer: {
        alignItems: 'center',
        marginLeft: 10,
        marginTop: 20,
      },
    }),
  };
};
