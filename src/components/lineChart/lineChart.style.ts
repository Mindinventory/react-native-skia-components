import { StyleSheet } from 'react-native';

export const lineChartStyle = () => {
  return {
    canvasStyles: StyleSheet.create({
      canvasContainer: {
        marginLeft: 10,
        marginTop: 20,
      },
    }),
  };
};
