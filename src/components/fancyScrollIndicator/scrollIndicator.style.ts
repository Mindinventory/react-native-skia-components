import { StyleSheet } from 'react-native';

export const indicatorStyle = () => {
  return StyleSheet.create({
    rowContainer: {
      backgroundColor: 'red',
      height: 100,
      margin: 10,
      width: 300,
    },
  });
};
