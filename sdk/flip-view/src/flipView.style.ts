import { StyleSheet } from 'react-native';

export const flipViewStyle = () => {
  return StyleSheet.create({
    cardContainer: {
      alignItems: 'center',
      bottom: 0,
      flex: 1,
      justifyContent: 'center',
      left: 0,
      position: 'absolute',
      right: 0,
      top: 0,
    },
  });
};
